{
  "name": "block",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server": "tsx server/index.ts",
    "start": "npm run build && npm run server",
    "setup-samples": "git clone https://github.com/felixroos/dough-samples.git && cd dough-samples && git submodule update --init"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "@types/ws": "^8.5.10",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "ws": "^8.18.0"
  }
}
